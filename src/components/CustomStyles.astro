---
import '@fontsource/inter/variable.css';

// Nunito
// Dosis
---

<style is:inline is:global>
  :root {
    --aw-font-sans: 'InterVariable';
    --aw-font-serif: var(--aw-font-sans);
    --aw-font-heading: var(--aw-font-sans);

    --aw-color-primary: #007A33;
    --aw-color-secondary: #007A33;
    --aw-color-accent: #007A33;
    --aw-color-text-page: #100007;
    --aw-color-text-muted: #100007;
    --aw-color-bg-page: #FFF3B0;
  }
</style>
